#include <iostream>
#include <algorithm>
#include <vector>


using namespace std;

int main(void){
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int N;
    cin >> N;
    int number[N];
    
    for(int i = 0; i < N; i++){
        cin >> number[i];
    }//숫자 전부 집어넣기

    int X;
    cin >> X;
    






}