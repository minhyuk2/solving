#include <bits/stdc++.h>
using namespace std;


int main(void){

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N;
    cin >> N;

    if(N == 0){
        cout << "YONSEI" << "\n";
    }else{
        cout << "Leading the Way to the Future\n";
    }
}